# Notes

THe `sample` directory contains files used for testing.

To execute local testing, run the following command. This will pickup the config from the package.json

```bash
node ./dist/cli.js
```

Commands for packing and publishing to NPM.

```bash
npm pack
npm login
npm whoami
npm publish
npm logout
```
